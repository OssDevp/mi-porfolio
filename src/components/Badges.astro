---
interface Props {
  href: string;
}

const { href } = Astro.props;
---

<a
  href={href}
  class="inline-flex items-center gap-2 py-2 px-3 bg-gray-800 text-white text-base font-medium rounded border border-red-400 hover:scale-105 cursor-pointer transition"
>
  <slot />
</a>
